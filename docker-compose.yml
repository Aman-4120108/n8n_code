services:
  n8n:
    build: .
    container_name: n8n
    restart: unless-stopped

    ports:
      - "5678:5678"

    environment:
      - TZ=Asia/Kolkata

    volumes:
      - ./data:/home/node/.n8n